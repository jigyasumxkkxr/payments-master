

export const Subheading = ({label}) => {
    return (
        <div className="text-sm font-normal text-slate-500 p-1 flex items-center justify-center mb-6">
            {label}
        </div>
    )
}